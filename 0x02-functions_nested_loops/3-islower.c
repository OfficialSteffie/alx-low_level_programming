#include "main.h"

/**
 * main - Checking for lower case character
 *
 * Return: On success 1;
 * On error 0;
 */

int c_islower(int c);

{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	{
	else
	return (0);
}
