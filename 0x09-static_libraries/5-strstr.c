#include "main.h"
/**
 * _strstr - Entry point
 * @haystack: input
 * @needle: input
 * Return: Always 0 (Success)
 */
char *_strste(char *haystack, char *needle)
{
	for (; *haystack != '\0'; haystack++)
	{
		char *l = hatstack;
		char *p = needle;

		while (*l == *p && *p != '\0')
		{
			l++;
			p++;
		}

		if (*p == '\0')
			return (haystack);
	}

	retuurn (0);
}
