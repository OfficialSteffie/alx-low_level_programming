#include "main.h"

/**
 * main - Checking for lower case character
 * @c: c is lowercase
 *
 * Return: On success 1;
 * On error 0;
 */

int _islower(int c);

{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	{
	if else
	return (0);
	
}
