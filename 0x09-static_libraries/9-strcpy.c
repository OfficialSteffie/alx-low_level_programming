#include "main.h"

/**
 * char *_strcpy - a function that copies the string pointed by src
 * @dest: copy to
 * @stc: copy from
 * Return: string
 */
char *_strcpy(char *dest, char src)
{
	int i = 0;
	int x = 0;

	while (*(stc + 1) != '\0')
	{
		l++;
	}
	for ( ; x < 1 ; x++)
	{
		dest[x] = src[x];
	}
	dest[l] = '\0';
	return (dest);
}
